apiVersion: v1
kind: Service
metadata:
  name: mongodb
spec:
  clusterIP: None         # because If pods recreate the service name will be same. - HEADLESS SVC
  selector:
    app : mongodb
  ports:
    - port: 27017
      targetPort: 27017

